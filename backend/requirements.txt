# --- Core API (light; enough to boot the app + run all tests) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27            # TestClient dependency
python-multipart>=0.0.9  # UploadFile / form parsing
Pillow>=10.2          # image decode for detector/ocr

# --- Data / infra ---
SQLAlchemy>=2.0
psycopg2-binary>=2.9   # Postgres driver (docker)
redis>=5.0

# --- LLM (default provider) ---
google-generativeai>=0.5

# --- Auth + reporting (Phase 1; light) ---
python-jose[cryptography]>=3.3
bcrypt>=4.1             # password hashing (used directly; avoids passlib version drift)
email-validator>=2.1
reportlab>=4.1          # PDF invoice

# Heavy vision/OCR deps live in requirements-vision.txt (optional; large downloads).
# RAG + barcode optional deps live in requirements-ml.txt.

# --- Migrations ---
alembic>=1.13
